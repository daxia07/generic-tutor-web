# Content moved off-repo

Learner content is **not** tracked in git.

Default location:

```
~/workspace/generic-tutor-web/content/system-design/
```

Override with env:

```
TUTOR_DATA_DIR=/path/to/data
```

See `docs/DECISIONS.md` (ADR-001) and `docs/FEATURES.md`.
